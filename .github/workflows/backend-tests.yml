# Task D (excellent band) + Task C automation.
# CI: JUnit on every push/PR. CD: package WAR + frontend (no public host required).
name: Sunrise Dental Clinic CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  backend:
    name: Test billing rules and package WAR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - name: Run JUnit tests for bill total and dentist slot clash
        working-directory: backend
        run: mvn -B test
      - name: Package sunrise.war for Tomcat
        working-directory: backend
        run: mvn -B -DskipTests package
      - name: Upload sunrise.war
        uses: actions/upload-artifact@v4
        with:
          name: sunrise-war
          path: backend/target/sunrise.war

  frontend:
    name: Build reception staff UI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install and build React clinic screens
        working-directory: frontend
        run: |
          npm ci
          npm run build
      - name: Upload frontend dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist
