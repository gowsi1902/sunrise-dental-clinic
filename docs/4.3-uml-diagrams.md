# 4.3 UML diagrams (Task A — 20 marks)

Assumptions: reception **Staff** and **Admin** are actors; guests do not self-book; **Exit** is logout.

## Use case

```mermaid
flowchart LR
    Staff((Staff))
    Admin((Admin))
    subgraph clinic [Sunrise Dental Clinic]
        UC0[Login]
        UC1[Register appointment]
        UC2[Display appointment details]
        UC3[Calculate and print bill]
        UC4[Help]
        UC5[Exit system]
        UC6[Prevent double booking]
        UC7[View clinic reports]
        UC8[Manage staff accounts]
    end
    Staff --> UC0
    Staff --> UC1
    Staff --> UC2
    Staff --> UC3
    Staff --> UC4
    Staff --> UC5
    UC1 -.->|include| UC6
    UC3 -.->|include| UC2
    Admin --> UC0
    Admin --> UC1
    Admin --> UC7
    Admin --> UC8
    UC7 -.->|extend| UC2
```

- `<<include>>` Register appointment always checks the dentist slot.
- `<<include>>` Billing needs the appointment record.
- `<<extend>>` Admin reports optionally drill into a visit.

## Class diagram (core)

Visibility: `-` private, `+` public. Multiplicity and navigability are shown on associations. **Composition** (`*--`): a payment cannot exist without its appointment (deleted with the visit). **Aggregation** (`o--`): dentists and treatments exist independently of any one visit. **Navigability** arrows: an appointment knows its dentist and treatment; a dentist does not hold a list of appointments in memory (loaded via DAO).

```mermaid
classDiagram
    class User {
        -int id
        -String username
        -String passwordHash
        -Role role
        +isAdmin() boolean
        +withoutSecret() User
    }
    class Appointment {
        -int id
        -String appointmentNo
        -String patientName
        -String address
        -String contactNumber
        -int dentistId
        -int treatmentId
        -LocalDate appointmentDate
        -LocalTime appointmentTime
        -BigDecimal totalAmount
        -AppointmentStatus status
        +getAppointmentNo() String
    }
    class Dentist {
        -int id
        -String fullName
        -String specialization
        +getFullName() String
    }
    class Treatment {
        -int id
        -String name
        -BigDecimal treatmentFee
        -BigDecimal consultationFee
        +getTreatmentFee() BigDecimal
        +getConsultationFee() BigDecimal
    }
    class Payment {
        -int id
        -int appointmentId
        -BigDecimal amount
        -String paymentMethod
    }
    class AuditLog {
        -int id
        -String action
        -String details
    }
    class AppointmentService {
        +listAll() List~Appointment~
        +get(int id) Appointment
        +findByNumber(String number) Appointment
        +create(Appointment a, User actor) Appointment
        +update(int id, Appointment a, User actor) Appointment
        +changeStatus(int id, AppointmentStatus s, User actor) Appointment
        +delete(int id, User actor) void
        -validate(Appointment a) void
        -nextNumber() String
    }
    class BillingService {
        +getBill(int appointmentId) BillDto
        +recordPayment(int appointmentId, User actor) Payment
    }
    class ClinicCalculator {
        +total(BigDecimal treat, BigDecimal consult) BigDecimal
        +slotClash(...) boolean
        +requireFutureOrToday(...) void
    }
    Dentist "1" o-- "0..*" Appointment : treats
    Treatment "1" o-- "0..*" Appointment : priced as
    Appointment "1" *-- "0..*" Payment : settles
    User "1" o-- "0..*" AuditLog : records
    AppointmentService ..> Appointment
    AppointmentService ..> ClinicCalculator
    BillingService ..> ClinicCalculator
    BillingService ..> Payment
```

Assumption: patient name, address and phone sit on `Appointment` (the brief lists them with the visit), not a separate Patient class. That keeps registration as one use case and one form.

## Sequence — login

```mermaid
sequenceDiagram
    participant S as Staff
    participant V as Login.jsx
    participant C as LoginServlet
    participant A as AuthService
    participant D as UserDao
    S->>V: username + password
    V->>C: POST /api/login
    C->>A: login()
    A->>D: findByUsername
    A->>A: PasswordUtil.matches
    C->>C: session.setAttribute user
    C-->>V: 200 user without hash
```

## Sequence — register appointment

```mermaid
sequenceDiagram
    participant V as AddAppointment.jsx
    participant C as AppointmentServlet
    participant S as AppointmentService
    participant D as AppointmentDao
    V->>C: POST /api/appointments
    C->>S: create()
    S->>S: validate + ClinicCalculator.total
    S->>D: isSlotFree
    alt clash
        S-->>C: 409 double booking
    else free
        S->>D: insert SDC-nnnn
        C-->>V: 201 appointment
    end
```

## Sequence — search and bill

```mermaid
sequenceDiagram
    participant V as Dashboard
    participant C as AppointmentServlet
    participant B as BillingServlet
    V->>C: GET /api/appointments?number=SDC-0001
    C-->>V: patient and visit fields
    V->>B: GET /api/billing/{id}
    B-->>V: treatment fee + consultation fee
```

## Design justification

Use cases map 1:1 to the six brief functions plus anti-double-booking (the problem stated in the scenario). Sequence diagrams cover login, register, and display/bill — three identified use cases as the marking scheme asks.
